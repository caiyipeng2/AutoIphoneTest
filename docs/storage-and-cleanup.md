# Storage and cleanup

Reports, screenshots, videos, artifacts, and logs are stored below `data`. The Overview and Settings pages show free space, retention thresholds, and cleanup previews. Cleanup requires a fresh confirmation token, records an audit event, and moves eligible files to the local trash area before removal.
